OpenAI Codex v0.142.5
--------
workdir: /Users/gio/Projects/camron-finlay-baseball
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019f38c8-ba8a-7a23-a067-b181b1aad675
--------
user
commit HEAD
exec
/bin/zsh -lc 'git status --short && git show --stat --oneline HEAD && git show --find-renames --find-copies --stat --patch HEAD' in /Users/gio/Projects/camron-finlay-baseball
 succeeded in 4863ms:
?? .neo/
99b6417 Camron Finlay baseball recruiting site — initial build
 .gitignore                          |   2 +
 CONTENT-TODO.md                     |  21 ++++
 README.md                           |  10 ++
 assets/photos/camron-comerica.jpg   | Bin 0 -> 205192 bytes
 assets/photos/championship-team.jpg | Bin 0 -> 173004 bytes
 css/styles.css                      | 189 +++++++++++++++++++++++++++++++++++
 data/schedule.json                  |   7 ++
 data/stats.json                     |  16 +++
 index.html                          | 194 ++++++++++++++++++++++++++++++++++++
 js/main.js                          | 144 ++++++++++++++++++++++++++
 10 files changed, 583 insertions(+)
commit 99b64170d4b8d637f2ae58f71fe49b1789be70bc
Author: Gio <gio@openclaw.local>
Date:   Mon Jul 6 12:50:30 2026 -0600

    Camron Finlay baseball recruiting site — initial build
    
    Sleek red-primary athlete profile: cinematic hero, HR ticker, champions band,
    about, stats, Outlook-synced schedule, recruiter form. Real photos, no placeholders.
    
    Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
---
 .gitignore                          |   2 +
 CONTENT-TODO.md                     |  21 ++++
 README.md                           |  10 ++
 assets/photos/camron-comerica.jpg   | Bin 0 -> 205192 bytes
 assets/photos/championship-team.jpg | Bin 0 -> 173004 bytes
 css/styles.css                      | 189 +++++++++++++++++++++++++++++++++++
 data/schedule.json                  |   7 ++
 data/stats.json                     |  16 +++
 index.html                          | 194 ++++++++++++++++++++++++++++++++++++
 js/main.js                          | 144 ++++++++++++++++++++++++++
 10 files changed, 583 insertions(+)

diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..ab3f4c4
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,2 @@
+assets/reference/
+.DS_Store
diff --git a/CONTENT-TODO.md b/CONTENT-TODO.md
new file mode 100644
index 0000000..b41ccb1
--- /dev/null
+++ b/CONTENT-TODO.md
@@ -0,0 +1,21 @@
+# Content to finalize — Camron Finlay Baseball
+
+The site is LIVE and fully functional with real photos. To make it 100% accurate and turn on the live features, provide:
+
+## From Ryan / the family
+1. **Real stats** — replace the starter numbers in `data/stats.json` (AVG, HR, RBI, SB) and the home-run log with Camron's actual figures.
+2. **More action photos** (optional, high-impact) — a batting/pitching/fielding action shot at high resolution would make an even stronger hero. Drop files in `assets/photos/` and swap in `index.html` / hero background.
+3. **Highlight video** — a Hudl or YouTube link to embed in the Highlights section.
+
+## To turn on the 3 live features
+4. **Home-run ticker (parent-updatable):** create a Google Sheet with rows like `homeRuns,14` and `hr,13,2-run shot vs rival`; publish to web as CSV; paste the CSV URL into `HR_SHEET_CSV_URL` in `js/main.js`. Then Ryan just edits the sheet to update the count.
+5. **Outlook calendar sync:** create a "Camron Baseball" calendar in Outlook, publish it, and paste the public `.ics` URL into `ICS_FEED_URL` in `js/main.js`. Games added in Outlook then appear automatically.
+6. **Recruiter form:** create a free key at https://web3forms.com for **ryanfinlay13@gmail.com**, paste it into `WEB3FORMS_ACCESS_KEY` in `js/main.js`. Submissions then email straight to Ryan.
+
+## Domain
+7. Purchase **camronfinlaybaseball.com**, then tell Gio — he'll add the CNAME and point DNS so the site lives at the custom domain.
+
+## Confirm
+- Bats: **Switch** · Throws: **Right** (confirmed)
+- Positions: 1B / 3B / SS / P / OF (confirmed)
+- 5'5", 160 lbs, L'Anse Creuse Middle School Central (confirmed)
diff --git a/README.md b/README.md
new file mode 100644
index 0000000..6da725e
--- /dev/null
+++ b/README.md
@@ -0,0 +1,10 @@
+# Camron "CamBam" Finlay — Baseball Recruiting Site
+
+Personal athlete profile for **Camron Finlay** (Class of 2032, #8, switch-hitter, Detroit Prospects). Built to showcase Camron for college and pro recruiters.
+
+- **Live:** https://rfinlay.github.io/camron-finlay-baseball/ (custom domain camronfinlaybaseball.com pending)
+- **Stack:** static HTML/CSS/JS, FullCalendar, Web3Forms
+- **Features:** home-run ticker (Google Sheet driven), Outlook-synced game calendar, recruiter contact form
+- **Setup / to finalize:** see `CONTENT-TODO.md`
+
+Not affiliated with or endorsed by the Detroit Prospects organization.
diff --git a/assets/photos/camron-comerica.jpg b/assets/photos/camron-comerica.jpg
new file mode 100644
index 0000000..2266d31
Binary files /dev/null and b/assets/photos/camron-comerica.jpg differ
diff --git a/assets/photos/championship-team.jpg b/assets/photos/championship-team.jpg
new file mode 100644
index 0000000..514f598
Binary files /dev/null and b/assets/photos/championship-team.jpg differ
diff --git a/css/styles.css b/css/styles.css
new file mode 100644
index 0000000..af406d8
--- /dev/null
+++ b/css/styles.css
@@ -0,0 +1,189 @@
+/* ===== Camron Finlay Baseball — sleek athletic recruiting site ===== */
+:root{
+  --red:#C8102E;
+  --red-dark:#9E0C24;
+  --navy:#0B1F3A;
+  --ink:#0C0F14;
+  --ink-2:#141922;
+  --panel:#171D28;
+  --line:rgba(255,255,255,.08);
+  --white:#F5F7FA;
+  --silver:#9BA7B4;
+  --silver-2:#6B7686;
+  --maxw:1180px;
+  --shadow:0 20px 60px rgba(0,0,0,.45);
+  --radius:16px;
+}
+*{box-sizing:border-box;margin:0;padding:0}
+html{scroll-behavior:smooth}
+body{
+  font-family:'Inter',system-ui,sans-serif;
+  background:var(--ink);color:var(--white);
+  line-height:1.6;-webkit-font-smoothing:antialiased;overflow-x:hidden;
+}
+h1,h2,h3{line-height:1.02;letter-spacing:.01em}
+a{color:inherit;text-decoration:none}
+img{display:block;max-width:100%}
+section{position:relative}
+.reveal{opacity:0;transform:translateY(28px);transition:opacity .8s ease,transform .8s ease}
+.reveal.in{opacity:1;transform:none}
+
+/* ---- buttons ---- */
+.btn{display:inline-flex;align-items:center;gap:.5rem;font-family:'Barlow Condensed',sans-serif;
+  font-weight:700;font-size:1.05rem;letter-spacing:.06em;text-transform:uppercase;
+  padding:.85rem 1.6rem;border-radius:999px;border:2px solid transparent;cursor:pointer;transition:.25s;white-space:nowrap}
+.btn--sm{font-size:.9rem;padding:.6rem 1.1rem}
+.btn--primary{background:var(--red);color:#fff;box-shadow:0 10px 30px rgba(200,16,46,.35)}
+.btn--primary:hover{background:var(--red-dark);transform:translateY(-2px)}
+.btn--ghost{border-color:rgba(255,255,255,.25);color:#fff}
+.btn--ghost:hover{border-color:#fff;background:rgba(255,255,255,.06)}
+
+/* ---- nav ---- */
+.nav{position:fixed;top:0;left:0;right:0;z-index:50;display:flex;align-items:center;justify-content:space-between;
+  padding:1rem clamp(1rem,4vw,2.5rem);transition:.3s;background:transparent}
+.nav.scrolled{background:rgba(10,13,18,.92);backdrop-filter:blur(12px);border-bottom:1px solid var(--line);padding-top:.7rem;padding-bottom:.7rem}
+.nav__brand{display:flex;align-items:center;gap:.6rem;font-family:'Barlow Condensed',sans-serif;font-weight:700;letter-spacing:.12em;font-size:1.05rem}
+.nav__num{display:grid;place-items:center;width:34px;height:34px;background:var(--red);color:#fff;border-radius:8px;font-family:'Anton';font-size:1.2rem}
+.nav__links{display:flex;align-items:center;gap:1.8rem;font-family:'Barlow Condensed',sans-serif;font-weight:600;letter-spacing:.05em;text-transform:uppercase;font-size:1rem}
+.nav__links a{color:var(--silver);transition:.2s}
+.nav__links a:hover{color:#fff}
+.nav__cta{color:#fff !important;background:var(--red);padding:.5rem 1.1rem;border-radius:999px}
+.nav__cta:hover{background:var(--red-dark)}
+.nav__toggle{display:none;flex-direction:column;gap:5px;background:none;border:0;cursor:pointer}
+.nav__toggle span{width:26px;height:2px;background:#fff;transition:.3s}
+
+/* ---- hero ---- */
+.hero{min-height:100vh;display:flex;flex-direction:column;justify-content:center;padding:6rem clamp(1rem,6vw,5rem) 4rem;overflow:hidden}
+.hero__bg{position:absolute;inset:0;background:url('../assets/photos/camron-comerica.jpg') center 22%/cover no-repeat;transform:scale(1.05);z-index:-2}
+.hero__overlay{position:absolute;inset:0;z-index:-1;
+  background:linear-gradient(100deg,rgba(9,12,17,.94) 0%,rgba(9,12,17,.75) 42%,rgba(9,12,17,.35) 70%,rgba(200,16,46,.25) 100%)}
+.hero__inner{max-width:var(--maxw);width:100%;margin:0 auto}
+.hero__eyebrow{font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.32em;color:var(--red);font-weight:700;font-size:1rem;margin-bottom:1rem}
+.hero__title{font-family:'Anton';font-size:clamp(3.5rem,13vw,10rem);letter-spacing:.01em;text-shadow:0 8px 40px rgba(0,0,0,.5)}
+.hero__sub{font-family:'Barlow Condensed',sans-serif;font-weight:700;font-size:clamp(1.3rem,3vw,2rem);letter-spacing:.08em;margin-top:.6rem;text-transform:uppercase}
+.hero__pos{font-family:'Barlow Condensed',sans-serif;color:var(--silver);letter-spacing:.14em;text-transform:uppercase;font-size:clamp(.95rem,1.6vw,1.15rem);margin-top:.5rem}
+.hero__tag{max-width:38ch;color:var(--white);opacity:.85;margin-top:1.4rem;font-size:1.1rem}
+.hero__actions{display:flex;gap:1rem;margin-top:2.2rem;flex-wrap:wrap}
+.hero__scroll{position:absolute;bottom:1.4rem;left:50%;transform:translateX(-50%);font-family:'Barlow Condensed',sans-serif;letter-spacing:.2em;text-transform:uppercase;font-size:.8rem;color:var(--silver);display:flex;flex-direction:column;align-items:center;gap:.5rem}
+.hero__scroll span{width:1px;height:34px;background:linear-gradient(var(--red),transparent);animation:drop 1.8s infinite}
+@keyframes drop{0%{opacity:0;transform:scaleY(.3)}50%{opacity:1}100%{opacity:0;transform:scaleY(1) translateY(6px)}}
+
+/* ---- metrics strip ---- */
+.metrics{display:grid;grid-template-columns:repeat(6,1fr);background:var(--red);position:relative;z-index:2}
+.metric{padding:1.5rem 1rem;text-align:center;border-right:1px solid rgba(255,255,255,.14)}
+.metric:last-child{border-right:0}
+.metric__val{display:block;font-family:'Anton';font-size:clamp(1.6rem,3.2vw,2.6rem);line-height:1}
+.metric__key{display:block;font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.14em;font-size:.8rem;opacity:.85;margin-top:.35rem}
+.metric--hr{background:rgba(0,0,0,.22)}
+
+/* ---- section shared ---- */
+.section__head{max-width:var(--maxw);margin:0 auto;padding:0 clamp(1rem,5vw,2rem) 2.5rem;text-align:center}
+.section__eyebrow{font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.3em;color:var(--red);font-weight:700;font-size:.95rem;margin-bottom:.6rem}
+.section__title{font-family:'Anton';font-size:clamp(2.2rem,6vw,4rem)}
+.section__note{color:var(--silver);margin-top:.7rem}
+.block__title{font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.1em;font-size:1.4rem;margin-bottom:1.1rem;color:#fff}
+
+/* ---- champions band ---- */
+.champs{min-height:80vh;display:flex;align-items:center;overflow:hidden}
+.champs__media{position:absolute;inset:0;background:url('../assets/photos/championship-team.jpg') center 30%/cover no-repeat;z-index:-2;filter:saturate(1.05)}
+.champs__overlay{position:absolute;inset:0;z-index:-1;background:linear-gradient(180deg,rgba(9,12,17,.72),rgba(9,12,17,.5)),radial-gradient(circle at 30% 50%,rgba(200,16,46,.35),transparent 60%)}
+.champs__inner{max-width:var(--maxw);margin:0 auto;padding:5rem clamp(1rem,6vw,3rem)}
+.champs__label{font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.3em;color:#fff;font-weight:700;margin-bottom:.7rem}
+.champs__title{font-family:'Anton';font-size:clamp(2.6rem,8vw,6rem);text-shadow:0 8px 40px rgba(0,0,0,.5)}
+.champs__text{max-width:44ch;color:var(--white);opacity:.9;margin-top:1.2rem;font-size:1.15rem}
+
+/* ---- about ---- */
+.about{padding:6rem clamp(1rem,6vw,3rem);background:var(--ink)}
+.about__grid{max-width:var(--maxw);margin:0 auto;display:grid;grid-template-columns:.85fr 1.15fr;gap:clamp(2rem,5vw,4.5rem);align-items:center}
+.about__photo{position:relative;border-radius:var(--radius);overflow:hidden;box-shadow:var(--shadow)}
+.about__photo img{width:100%;height:100%;object-fit:cover;aspect-ratio:3/4}
+.about__photo::after{content:"";position:absolute;inset:0;border:1px solid var(--line);border-radius:var(--radius)}
+.about__badge{position:absolute;left:1rem;bottom:1rem;background:var(--red);font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.08em;font-weight:700;padding:.4rem .9rem;border-radius:999px;font-size:.9rem}
+.about__copy p{color:var(--silver);margin-top:1rem}
+.about__copy strong{color:#fff}
+.about__pg{display:inline-block;margin-top:1.6rem;font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.08em;font-weight:700;color:var(--red);border-bottom:2px solid var(--red);padding-bottom:2px}
+
+/* ---- stats ---- */
+.stats{padding:6rem clamp(1rem,6vw,3rem);background:var(--ink-2)}
+.stats__cards{max-width:var(--maxw);margin:0 auto;display:grid;grid-template-columns:repeat(4,1fr);gap:1rem}
+.statcard{background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);padding:1.6rem;text-align:center;position:relative;overflow:hidden}
+.statcard::before{content:"";position:absolute;top:0;left:0;width:100%;height:4px;background:var(--red)}
+.statcard__val{font-family:'Anton';font-size:clamp(2rem,4vw,3rem);line-height:1}
+.statcard__key{font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.12em;color:var(--silver);font-size:.85rem;margin-top:.4rem}
+.stats__lower{max-width:var(--maxw);margin:2.5rem auto 0;display:grid;grid-template-columns:1fr 1fr;gap:2rem}
+.hrlog,.highlights{background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);padding:1.8rem}
+.hrlog__list{list-style:none}
+.hrlog__list li{display:flex;justify-content:space-between;gap:1rem;padding:.75rem 0;border-bottom:1px solid var(--line);color:var(--silver)}
+.hrlog__list li:last-child{border-bottom:0}
+.hrlog__list b{color:#fff;font-family:'Barlow Condensed',sans-serif;letter-spacing:.04em}
+.hrlog__list .hr__no{color:var(--red);font-family:'Anton'}
+.highlights__frame{aspect-ratio:16/9;border-radius:12px;overflow:hidden;background:#0a0d12;border:1px solid var(--line);margin-bottom:1.2rem}
+.highlights__frame iframe{width:100%;height:100%;border:0}
+.highlights__empty{height:100%;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:.6rem;color:var(--silver-2)}
+.highlights__empty span{width:60px;height:60px;display:grid;place-items:center;border-radius:50%;background:var(--red);color:#fff;font-size:1.4rem}
+
+/* ---- schedule ---- */
+.schedule{padding:6rem clamp(1rem,6vw,3rem);background:var(--ink)}
+.schedule__grid{max-width:var(--maxw);margin:0 auto;display:grid;grid-template-columns:1.5fr .9fr;gap:2rem;align-items:start}
+.schedule__cal{background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);padding:1.4rem}
+.schedule__up{background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);padding:1.8rem}
+.upcoming{list-style:none}
+.upcoming li{padding:.9rem 0;border-bottom:1px solid var(--line)}
+.upcoming li:last-child{border-bottom:0}
+.upcoming__date{font-family:'Anton';color:var(--red);font-size:1.1rem}
+.upcoming__game{font-family:'Barlow Condensed',sans-serif;font-weight:600;letter-spacing:.03em;color:#fff;font-size:1.1rem}
+.upcoming__meta{color:var(--silver);font-size:.9rem}
+/* FullCalendar dark theming */
+.fc{--fc-border-color:var(--line);--fc-page-bg-color:transparent;color:var(--white)}
+.fc .fc-toolbar-title{font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.05em}
+.fc .fc-button-primary{background:var(--red);border-color:var(--red);text-transform:uppercase;font-family:'Barlow Condensed',sans-serif;letter-spacing:.05em}
+.fc .fc-button-primary:hover{background:var(--red-dark);border-color:var(--red-dark)}
+.fc .fc-button-primary:not(:disabled).fc-button-active{background:var(--navy);border-color:var(--navy)}
+.fc .fc-daygrid-day.fc-day-today{background:rgba(200,16,46,.12)}
+.fc .fc-event{background:var(--red);border-color:var(--red);font-size:.78rem;padding:1px 3px}
+.fc a{color:var(--white)}
+
+/* ---- recruiters ---- */
+.recruiters{padding:6rem clamp(1rem,6vw,3rem);background:linear-gradient(160deg,var(--navy),var(--ink) 70%)}
+.recruiters__grid{max-width:var(--maxw);margin:0 auto;display:grid;grid-template-columns:1fr 1.1fr;gap:clamp(2rem,5vw,4rem);align-items:start}
+.recruiters__pitch p{color:var(--silver);margin-top:1rem}
+.recruiters__facts{list-style:none;margin:1.6rem 0}
+.recruiters__facts li{display:flex;justify-content:space-between;padding:.7rem 0;border-bottom:1px solid var(--line);font-family:'Barlow Condensed',sans-serif;letter-spacing:.03em;font-size:1.1rem}
+.recruiters__facts span{color:var(--silver);text-transform:uppercase;letter-spacing:.1em;font-size:.85rem;align-self:center}
+.rform{background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);padding:2rem;box-shadow:var(--shadow)}
+.rform__row{display:grid;grid-template-columns:1fr 1fr;gap:1rem}
+.rform label{display:block;font-family:'Barlow Condensed',sans-serif;text-transform:uppercase;letter-spacing:.08em;font-size:.85rem;color:var(--silver);margin-top:1rem}
+.rform input,.rform select,.rform textarea{width:100%;margin-top:.4rem;background:var(--ink);border:1px solid var(--line);border-radius:10px;padding:.75rem .9rem;color:#fff;font-family:'Inter',sans-serif;font-size:.95rem;transition:.2s}
+.rform input:focus,.rform select:focus,.rform textarea:focus{outline:0;border-color:var(--red);box-shadow:0 0 0 3px rgba(200,16,46,.2)}
+.rform button{margin-top:1.5rem;width:100%;justify-content:center}
+.rform__status{margin-top:1rem;text-align:center;font-family:'Barlow Condensed',sans-serif;letter-spacing:.05em;min-height:1.2rem}
+.rform__status.ok{color:#3ad07a}
+.rform__status.err{color:#ff6b6b}
+
+/* ---- footer ---- */
+.footer{background:#06080c;padding:3.5rem clamp(1rem,6vw,3rem);border-top:1px solid var(--line)}
+.footer__inner{max-width:var(--maxw);margin:0 auto;text-align:center}
+.footer__brand{display:inline-flex;align-items:center;gap:.6rem;font-family:'Barlow Condensed',sans-serif;font-weight:700;letter-spacing:.12em;font-size:1.2rem;margin-bottom:1rem}
+.footer__contact{color:var(--silver)}
+.footer__contact a{color:var(--red)}
+.footer__disc{max-width:70ch;margin:1.2rem auto 0;color:var(--silver-2);font-size:.82rem;line-height:1.6}
+.footer__copy{margin-top:1rem;color:var(--silver-2);font-size:.82rem}
+
+/* ---- responsive ---- */
+@media(max-width:900px){
+  .nav__links{position:fixed;inset:0 0 auto 0;top:0;flex-direction:column;background:rgba(10,13,18,.98);padding:6rem 2rem 2rem;gap:1.4rem;transform:translateY(-100%);transition:.35s;height:100vh;justify-content:flex-start;font-size:1.3rem}
+  .nav__links.open{transform:none}
+  .nav__toggle{display:flex;z-index:60}
+  .metrics{grid-template-columns:repeat(3,1fr)}
+  .metric:nth-child(3){border-right:0}
+  .metric:nth-child(1),.metric:nth-child(2),.metric:nth-child(3){border-bottom:1px solid rgba(255,255,255,.14)}
+  .about__grid,.stats__lower,.schedule__grid,.recruiters__grid{grid-template-columns:1fr}
+  .stats__cards{grid-template-columns:repeat(2,1fr)}
+  .about__photo img{aspect-ratio:4/5}
+}
+@media(max-width:520px){
+  .rform__row{grid-template-columns:1fr}
+  .metrics{grid-template-columns:repeat(2,1fr)}
+  .metric{border-right:0!important}
+  .hero__actions .btn{flex:1;justify-content:center}
+}
diff --git a/data/schedule.json b/data/schedule.json
new file mode 100644
index 0000000..ba0c3c3
--- /dev/null
+++ b/data/schedule.json
@@ -0,0 +1,7 @@
+[
+  { "title": "Prospects vs. Michigan Bulls", "start": "2026-07-12", "location": "Clinton Township, MI" },
+  { "title": "Summer Classic — Pool Play", "start": "2026-07-19", "location": "Macomb, MI" },
+  { "title": "Summer Classic — Bracket", "start": "2026-07-20", "location": "Macomb, MI" },
+  { "title": "Prospects vs. Motor City Hit Dogs", "start": "2026-08-02", "location": "Detroit, MI" },
+  { "title": "End-of-Season Tournament", "start": "2026-08-09", "location": "Ann Arbor, MI" }
+]
diff --git a/data/stats.json b/data/stats.json
new file mode 100644
index 0000000..6876f27
--- /dev/null
+++ b/data/stats.json
@@ -0,0 +1,16 @@
+{
+  "season": "2026 Travel Season",
+  "homeRuns": 14,
+  "cards": [
+    { "k": "AVG", "v": ".412" },
+    { "k": "Home Runs", "v": "14" },
+    { "k": "RBI", "v": "38" },
+    { "k": "Stolen Bases", "v": "21" }
+  ],
+  "hrLog": [
+    { "n": 14, "note": "Championship weekend blast", "date": "Jun 2026" },
+    { "n": 13, "note": "2-run shot vs. rival club", "date": "Jun 2026" },
+    { "n": 12, "note": "Opposite-field bomb", "date": "May 2026" }
+  ],
+  "_note": "STARTER NUMBERS — replace with Camron's real stats. Keep this file current, or connect a Google Sheet via HR_SHEET_CSV_URL in js/main.js."
+}
diff --git a/index.html b/index.html
new file mode 100644
index 0000000..03d64d2
--- /dev/null
+++ b/index.html
@@ -0,0 +1,194 @@
+<!DOCTYPE html>
+<html lang="en">
+<head>
+<meta charset="UTF-8" />
+<meta name="viewport" content="width=device-width, initial-scale=1.0" />
+<title>Camron "CamBam" Finlay | Class of 2032 · #8 · Switch-Hitter</title>
+<meta name="description" content="Official athlete profile of Camron 'CamBam' Finlay — versatile switch-hitting infielder, Class of 2032, Detroit Prospects. Recruiting information for college and pro scouts." />
+<link rel="preconnect" href="https://fonts.googleapis.com" />
+<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
+<link href="https://fonts.googleapis.com/css2?family=Anton&family=Barlow+Condensed:wght@500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
+<link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.css" rel="stylesheet" />
+<link rel="stylesheet" href="css/styles.css" />
+<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' rx='18' fill='%23C8102E'/><text x='50' y='72' font-size='60' font-family='Arial' font-weight='bold' fill='white' text-anchor='middle'>8</text></svg>" />
+</head>
+<body>
+
+<!-- NAV -->
+<header class="nav" id="nav">
+  <a href="#home" class="nav__brand">
+    <span class="nav__num">8</span>
+    <span class="nav__name">CAMRON FINLAY</span>
+  </a>
+  <nav class="nav__links">
+    <a href="#about">About</a>
+    <a href="#stats">Stats</a>
+    <a href="#schedule">Schedule</a>
+    <a href="#recruiters" class="nav__cta">For Recruiters</a>
+  </nav>
+  <button class="nav__toggle" aria-label="Menu" id="navToggle"><span></span><span></span><span></span></button>
+</header>
+
+<!-- HERO -->
+<section class="hero" id="home">
+  <div class="hero__bg"></div>
+  <div class="hero__overlay"></div>
+  <div class="hero__inner">
+    <p class="hero__eyebrow">"CamBam" · Detroit, Michigan</p>
+    <h1 class="hero__title">CAMRON<br/>FINLAY</h1>
+    <p class="hero__sub">Class of 2032 &nbsp;·&nbsp; #8 &nbsp;·&nbsp; Switch-Hitter</p>
+    <p class="hero__pos">1B · 3B · SS · P · OF &nbsp;—&nbsp; Detroit Prospects</p>
+    <p class="hero__tag">A young utility player with a big-league dream and the work ethic to chase it.</p>
+    <div class="hero__actions">
+      <a href="#recruiters" class="btn btn--primary">For Recruiters →</a>
+      <a href="#stats" class="btn btn--ghost">View the Numbers</a>
+    </div>
+  </div>
+  <div class="hero__scroll"><span></span>Scroll</div>
+</section>
+
+<!-- MEASURABLES + HR TICKER -->
+<section class="metrics">
+  <div class="metric"><span class="metric__val">2032</span><span class="metric__key">Class</span></div>
+  <div class="metric"><span class="metric__val">5'5"</span><span class="metric__key">Height</span></div>
+  <div class="metric"><span class="metric__val">160</span><span class="metric__key">Weight (lbs)</span></div>
+  <div class="metric"><span class="metric__val">Switch</span><span class="metric__key">Bats</span></div>
+  <div class="metric"><span class="metric__val">Right</span><span class="metric__key">Throws</span></div>
+  <div class="metric metric--hr"><span class="metric__val" id="hrTicker">0</span><span class="metric__key">Home Runs</span></div>
+</section>
+
+<!-- CHAMPIONS BAND -->
+<section class="champs" id="champs">
+  <div class="champs__media"></div>
+  <div class="champs__overlay"></div>
+  <div class="champs__inner reveal">
+    <p class="champs__label">Detroit Prospects</p>
+    <h2 class="champs__title">2026 Tournament<br/>Champions</h2>
+    <p class="champs__text">Camron and the Detroit Prospects brought home the hardware in 2026 — one milestone on a long road ahead.</p>
+  </div>
+</section>
+
+<!-- ABOUT -->
+<section class="about" id="about">
+  <div class="about__grid">
+    <div class="about__photo reveal">
+      <img src="assets/photos/camron-comerica.jpg" alt="Camron Finlay in his #8 Detroit Prospects uniform at the ballpark" loading="lazy" />
+      <span class="about__badge">#8 · Detroit Prospects</span>
+    </div>
+    <div class="about__copy reveal">
+      <p class="section__eyebrow">The Player</p>
+      <h2 class="section__title">Meet CamBam</h2>
+      <p>Camron "CamBam" Finlay is a 12-year-old switch-hitting infielder from the Detroit area with a big-league dream and the discipline to chase it. A true five-position utility player — first base, third base, shortstop, pitcher, and the outfield — Camron gives a coaching staff a Swiss-army-knife option almost anywhere on the diamond.</p>
+      <p>He plays travel ball for the <strong>Detroit Prospects</strong>, where in 2026 his team captured a tournament championship. Switch-hitting from both sides of the plate and comfortable all over the field, he profiles as the kind of versatile, coachable player who keeps finding ways to help his team win.</p>
+      <p>Off the field, teammates, parents, and coaches know him as respectful, kind, and endlessly enthusiastic about the game. He is <strong>Class of 2032</strong> — just getting started, with his sights set on college baseball and, one day, the pros.</p>
+      <a href="https://www.perfectgame.org/Players/Playerprofile.aspx?ID=1699392" target="_blank" rel="noopener" class="about__pg">View Perfect Game Profile ↗</a>
+    </div>
+  </div>
+</section>
+
+<!-- STATS & HIGHLIGHTS -->
+<section class="stats" id="stats">
+  <div class="section__head reveal">
+    <p class="section__eyebrow">By the Numbers</p>
+    <h2 class="section__title">Stats &amp; Highlights</h2>
+    <p class="section__note">Live season numbers, kept current by the family.</p>
+  </div>
+
+  <div class="stats__cards" id="statCards"><!-- injected by JS --></div>
+
+  <div class="stats__lower">
+    <div class="hrlog reveal">
+      <h3 class="block__title">Home Run Log</h3>
+      <ul class="hrlog__list" id="hrLog"><!-- injected by JS --></ul>
+    </div>
+    <div class="highlights reveal">
+      <h3 class="block__title">Highlight Reel</h3>
+      <div class="highlights__frame" id="highlightSlot">
+        <div class="highlights__empty">
+          <span>▶</span>
+          <p>Highlight video coming soon</p>
+        </div>
+      </div>
+      <a href="https://www.perfectgame.org/Players/Playerprofile.aspx?ID=1699392" target="_blank" rel="noopener" class="btn btn--ghost btn--sm">Perfect Game Metrics ↗</a>
+    </div>
+  </div>
+</section>
+
+<!-- SCHEDULE -->
+<section class="schedule" id="schedule">
+  <div class="section__head reveal">
+    <p class="section__eyebrow">On the Diamond</p>
+    <h2 class="section__title">Game Schedule</h2>
+    <p class="section__note">Synced from the team calendar. Recruiters welcome at any game.</p>
+  </div>
+  <div class="schedule__grid">
+    <div class="schedule__cal reveal"><div id="calendar"></div></div>
+    <aside class="schedule__up reveal">
+      <h3 class="block__title">Upcoming Games</h3>
+      <ul class="upcoming" id="upcoming"><!-- injected by JS --></ul>
+    </aside>
+  </div>
+</section>
+
+<!-- RECRUITERS / CONTACT -->
+<section class="recruiters" id="recruiters">
+  <div class="recruiters__grid">
+    <div class="recruiters__pitch reveal">
+      <p class="section__eyebrow">For College &amp; Pro Scouts</p>
+      <h2 class="section__title">Recruit Camron</h2>
+      <p>Interested in Camron for your program, camp, showcase, or scouting list? Reach out and Ryan Finlay (Camron's dad) will follow up with film, schedule, and measurables.</p>
+      <ul class="recruiters__facts">
+        <li><span>Class</span> 2032</li>
+        <li><span>Positions</span> 1B / 3B / SS / P / OF</li>
+        <li><span>Bats / Throws</span> Switch / Right</li>
+        <li><span>Height / Weight</span> 5'5" / 160 lbs</li>
+        <li><span>Club</span> Detroit Prospects (MI)</li>
+      </ul>
+      <a href="#" class="btn btn--ghost btn--sm" id="onePager">Download One-Pager (PDF)</a>
+    </div>
+
+    <form class="rform reveal" id="recruitForm">
+      <div class="rform__row">
+        <label>Name<input type="text" name="name" required /></label>
+        <label>Organization<input type="text" name="organization" required /></label>
+      </div>
+      <div class="rform__row">
+        <label>You are a<select name="type" required>
+          <option value="">Select…</option>
+          <option>College Coach</option>
+          <option>MLB / Pro Scout</option>
+          <option>Scouting Service</option>
+          <option>Camp / Showcase</option>
+          <option>Other</option>
+        </select></label>
+        <label>Interest Level<select name="interest">
+          <option>Just learning about Camron</option>
+          <option>Following his progress</option>
+          <option>Actively recruiting</option>
+        </select></label>
+      </div>
+      <div class="rform__row">
+        <label>Email<input type="email" name="email" required /></label>
+        <label>Phone<input type="tel" name="phone" /></label>
+      </div>
+      <label>Message<textarea name="message" rows="4" required></textarea></label>
+      <button type="submit" class="btn btn--primary" id="rformBtn">Send to Camron's Family</button>
+      <p class="rform__status" id="rformStatus"></p>
+    </form>
+  </div>
+</section>
+
+<!-- FOOTER -->
+<footer class="footer">
+  <div class="footer__inner">
+    <div class="footer__brand"><span class="nav__num">8</span> CAMRON "CAMBAM" FINLAY</div>
+    <p class="footer__contact">Recruiting contact: <a href="mailto:ryanfinlay13@gmail.com">ryanfinlay13@gmail.com</a></p>
+    <p class="footer__disc">This is a personal athlete profile for Camron Finlay. It is not affiliated with, endorsed by, or an official site of the Detroit Prospects organization. All team names are used for factual, identifying purposes only.</p>
+    <p class="footer__copy">© <span id="yr"></span> Camron Finlay Baseball · Built with pride for a future pro.</p>
+  </div>
+</footer>
+
+<script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js"></script>
+<script src="js/main.js"></script>
+</body>
+</html>
diff --git a/js/main.js b/js/main.js
new file mode 100644
index 0000000..85a6915
--- /dev/null
+++ b/js/main.js
@@ -0,0 +1,144 @@
+/* ===== Camron Finlay Baseball ===== */
+
+/* ---- CONFIG: connect these when ready (see CONTENT-TODO.md) ---- */
+const HR_SHEET_CSV_URL   = ""; // published Google Sheet CSV for live HR count (blank = use data/stats.json)
+const ICS_FEED_URL       = ""; // published Outlook calendar .ics feed (blank = use data/schedule.json)
+const WEB3FORMS_ACCESS_KEY = "GET-FREE-KEY"; // create free key for ryanfinlay13@gmail.com at web3forms.com
+
+/* ---- nav: scroll state + mobile toggle ---- */
+const nav = document.getElementById('nav');
+addEventListener('scroll', () => nav.classList.toggle('scrolled', scrollY > 40));
+const toggle = document.getElementById('navToggle');
+const links = document.querySelector('.nav__links');
+toggle.addEventListener('click', () => links.classList.toggle('open'));
+links.querySelectorAll('a').forEach(a => a.addEventListener('click', () => links.classList.remove('open')));
+
+/* ---- year ---- */
+document.getElementById('yr').textContent = new Date().getFullYear();
+
+/* ---- scroll reveal ---- */
+const io = new IntersectionObserver((entries) => {
+  entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } });
+}, { threshold: .15 });
+document.querySelectorAll('.reveal').forEach(el => io.observe(el));
+
+/* ---- animated counter ---- */
+function animateCount(el, target){
+  const dur = 1400, t0 = performance.now();
+  function step(t){
+    const p = Math.min((t - t0)/dur, 1);
+    el.textContent = Math.round(target * (1 - Math.pow(1-p,3)));
+    if (p < 1) requestAnimationFrame(step);
+  }
+  requestAnimationFrame(step);
+}
+
+/* ---- stats: load from Google Sheet CSV or local fallback ---- */
+async function loadStats(){
+  let data = null;
+  if (HR_SHEET_CSV_URL){
+    try { data = parseSheet(await (await fetch(HR_SHEET_CSV_URL)).text()); } catch(e){}
+  }
+  if (!data){
+    try { data = await (await fetch('data/stats.json')).json(); } catch(e){ data = fallbackStats(); }
+  }
+  renderStats(data);
+}
+function fallbackStats(){
+  return { homeRuns:14, season:"2026 Travel Season",
+    cards:[{k:"AVG",v:".412"},{k:"Home Runs",v:"14"},{k:"RBI",v:"38"},{k:"Stolen Bases",v:"21"}],
+    hrLog:[{n:14,note:"Championship weekend blast",date:"Jun 2026"},{n:13,note:"2-run shot vs. rival club",date:"Jun 2026"},{n:12,note:"Opposite-field bomb",date:"May 2026"}] };
+}
+function parseSheet(csv){
+  // expects rows: key,value  — plus optional HR log rows prefixed "hr,"
+  const lines = csv.trim().split(/\r?\n/).map(r => r.split(','));
+  const map = {}; const cards = []; const hrLog = [];
+  lines.forEach(([a,b,c]) => {
+    if(!a) return;
+    const key = a.trim().toLowerCase();
+    if (key === 'hr'){ hrLog.push({ n:+b, note:(c||'').trim(), date:'' }); }
+    else if (key === 'homeruns'){ map.homeRuns = +b; cards.push({k:'Home Runs',v:b}); }
+    else { map[key]=b; cards.push({k:a.trim(),v:(b||'').trim()}); }
+  });
+  return { homeRuns: map.homeRuns || 0, cards, hrLog };
+}
+function renderStats(d){
+  const hr = document.getElementById('hrTicker');
+  const doCount = () => animateCount(hr, d.homeRuns || 0);
+  const hio = new IntersectionObserver((es)=>{es.forEach(e=>{if(e.isIntersecting){doCount();hio.disconnect();}})},{threshold:.4});
+  hio.observe(document.querySelector('.metrics'));
+
+  const cards = (d.cards && d.cards.length) ? d.cards : fallbackStats().cards;
+  document.getElementById('statCards').innerHTML = cards.slice(0,4).map(c =>
+    `<div class="statcard"><div class="statcard__val">${c.v}</div><div class="statcard__key">${c.k}</div></div>`).join('');
+
+  const log = (d.hrLog && d.hrLog.length) ? d.hrLog : fallbackStats().hrLog;
+  document.getElementById('hrLog').innerHTML = log.map(h =>
+    `<li><span class="hr__no">#${h.n}</span> <b>${h.note||'Home run'}</b> <span>${h.date||''}</span></li>`).join('');
+}
+
+/* ---- schedule: ICS feed or local fallback ---- */
+async function loadSchedule(){
+  const cal = new FullCalendar.Calendar(document.getElementById('calendar'), {
+    initialView:'dayGridMonth', height:'auto', firstDay:0,
+    headerToolbar:{left:'prev,next',center:'title',right:'today'},
+  });
+  let events = [];
+  if (ICS_FEED_URL){
+    cal.setOption('events', { url: ICS_FEED_URL, format:'ics' });
+  }
+  try {
+    events = await (await fetch('data/schedule.json')).json();
+  } catch(e){ events = []; }
+  if (!ICS_FEED_URL) events.forEach(ev => cal.addEvent(ev));
+  cal.render();
+  renderUpcoming(events);
+}
+function renderUpcoming(events){
+  const up = document.getElementById('upcoming');
+  const now = new Date();
+  const list = events.map(e => ({...e, d:new Date(e.start)}))
+    .filter(e => e.d >= new Date(now.getFullYear(),now.getMonth(),now.getDate()-1))
+    .sort((a,b)=>a.d-b.d).slice(0,5);
+  if (!list.length){ up.innerHTML = '<li class="upcoming__meta">Schedule updates coming soon — check back for game dates.</li>'; return; }
+  up.innerHTML = list.map(e => {
+    const dt = e.d.toLocaleDateString('en-US',{month:'short',day:'numeric'});
+    return `<li><div class="upcoming__date">${dt}</div><div class="upcoming__game">${e.title}</div><div class="upcoming__meta">${e.location||''}</div></li>`;
+  }).join('');
+}
+
+/* ---- recruiter form (Web3Forms) ---- */
+const form = document.getElementById('recruitForm');
+const status = document.getElementById('rformStatus');
+const btn = document.getElementById('rformBtn');
+form.addEventListener('submit', async (e) => {
+  e.preventDefault();
+  status.className = 'rform__status';
+  if (!WEB3FORMS_ACCESS_KEY || WEB3FORMS_ACCESS_KEY === 'GET-FREE-KEY'){
+    status.className = 'rform__status err';
+    status.textContent = 'Form not connected yet — email ryanfinlay13@gmail.com directly for now.';
+    return;
+  }
+  btn.disabled = true; btn.textContent = 'Sending…';
+  const fd = new FormData(form);
+  fd.append('access_key', WEB3FORMS_ACCESS_KEY);
+  fd.append('subject', 'New recruiter inquiry — Camron Finlay');
+  fd.append('from_name', 'CamronFinlayBaseball.com');
+  try {
+    const r = await fetch('https://api.web3forms.com/submit', { method:'POST', body:fd });
+    const j = await r.json();
+    if (j.success){ status.className='rform__status ok'; status.textContent='Thanks! Camron’s family will be in touch soon.'; form.reset(); }
+    else throw new Error();
+  } catch(err){ status.className='rform__status err'; status.textContent='Something went wrong — please email ryanfinlay13@gmail.com.'; }
+  btn.disabled = false; btn.textContent = 'Send to Camron’s Family';
+});
+
+/* ---- one-pager placeholder ---- */
+document.getElementById('onePager').addEventListener('click', (e) => {
+  e.preventDefault();
+  alert('One-page recruiting profile (PDF) coming soon. For now, use the form or email ryanfinlay13@gmail.com.');
+});
+
+/* ---- init ---- */
+loadStats();
+loadSchedule();

exec
/bin/zsh -lc "nl -ba js/main.js | sed -n '70,110p'; nl -ba index.html | sed -n '185,195p'" in /Users/gio/Projects/camron-finlay-baseball
 succeeded in 0ms:
    70	
    71	  const cards = (d.cards && d.cards.length) ? d.cards : fallbackStats().cards;
    72	  document.getElementById('statCards').innerHTML = cards.slice(0,4).map(c =>
    73	    `<div class="statcard"><div class="statcard__val">${c.v}</div><div class="statcard__key">${c.k}</div></div>`).join('');
    74	
    75	  const log = (d.hrLog && d.hrLog.length) ? d.hrLog : fallbackStats().hrLog;
    76	  document.getElementById('hrLog').innerHTML = log.map(h =>
    77	    `<li><span class="hr__no">#${h.n}</span> <b>${h.note||'Home run'}</b> <span>${h.date||''}</span></li>`).join('');
    78	}
    79	
    80	/* ---- schedule: ICS feed or local fallback ---- */
    81	async function loadSchedule(){
    82	  const cal = new FullCalendar.Calendar(document.getElementById('calendar'), {
    83	    initialView:'dayGridMonth', height:'auto', firstDay:0,
    84	    headerToolbar:{left:'prev,next',center:'title',right:'today'},
    85	  });
    86	  let events = [];
    87	  if (ICS_FEED_URL){
    88	    cal.setOption('events', { url: ICS_FEED_URL, format:'ics' });
    89	  }
    90	  try {
    91	    events = await (await fetch('data/schedule.json')).json();
    92	  } catch(e){ events = []; }
    93	  if (!ICS_FEED_URL) events.forEach(ev => cal.addEvent(ev));
    94	  cal.render();
    95	  renderUpcoming(events);
    96	}
    97	function renderUpcoming(events){
    98	  const up = document.getElementById('upcoming');
    99	  const now = new Date();
   100	  const list = events.map(e => ({...e, d:new Date(e.start)}))
   101	    .filter(e => e.d >= new Date(now.getFullYear(),now.getMonth(),now.getDate()-1))
   102	    .sort((a,b)=>a.d-b.d).slice(0,5);
   103	  if (!list.length){ up.innerHTML = '<li class="upcoming__meta">Schedule updates coming soon — check back for game dates.</li>'; return; }
   104	  up.innerHTML = list.map(e => {
   105	    const dt = e.d.toLocaleDateString('en-US',{month:'short',day:'numeric'});
   106	    return `<li><div class="upcoming__date">${dt}</div><div class="upcoming__game">${e.title}</div><div class="upcoming__meta">${e.location||''}</div></li>`;
   107	  }).join('');
   108	}
   109	
   110	/* ---- recruiter form (Web3Forms) ---- */
   185	    <p class="footer__contact">Recruiting contact: <a href="mailto:ryanfinlay13@gmail.com">ryanfinlay13@gmail.com</a></p>
   186	    <p class="footer__disc">This is a personal athlete profile for Camron Finlay. It is not affiliated with, endorsed by, or an official site of the Detroit Prospects organization. All team names are used for factual, identifying purposes only.</p>
   187	    <p class="footer__copy">© <span id="yr"></span> Camron Finlay Baseball · Built with pride for a future pro.</p>
   188	  </div>
   189	</footer>
   190	
   191	<script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js"></script>
   192	<script src="js/main.js"></script>
   193	</body>
   194	</html>

codex
The static fallback site largely works, but the advertised Outlook calendar sync path is broken/incomplete once configured. The live calendar feed cannot be parsed with the current dependencies, and the upcoming-games sidebar remains tied to local JSON.

Full review comments:

- [P2] Load the iCalendar plugin before using ICS feeds — /Users/gio/Projects/camron-finlay-baseball/js/main.js:88-88
  When `ICS_FEED_URL` is configured with an Outlook `.ics` URL, this calendar will not parse it with the scripts currently loaded: `index.html` only includes the main FullCalendar bundle, while `format: 'ics'` requires FullCalendar's iCalendar connector plus `ical.js`. As a result, the advertised Outlook-synced schedule will fail to populate from the feed.

- [P2] Sync upcoming games from the live calendar feed — /Users/gio/Projects/camron-finlay-baseball/js/main.js:95-95
  When `ICS_FEED_URL` is non-empty, `events` is still loaded only from `data/schedule.json`, and `renderUpcoming(events)` uses that local fallback data rather than the Outlook feed. This means the main calendar and the “Upcoming Games” sidebar will diverge once the live calendar is enabled.
The static fallback site largely works, but the advertised Outlook calendar sync path is broken/incomplete once configured. The live calendar feed cannot be parsed with the current dependencies, and the upcoming-games sidebar remains tied to local JSON.

Full review comments:

- [P2] Load the iCalendar plugin before using ICS feeds — /Users/gio/Projects/camron-finlay-baseball/js/main.js:88-88
  When `ICS_FEED_URL` is configured with an Outlook `.ics` URL, this calendar will not parse it with the scripts currently loaded: `index.html` only includes the main FullCalendar bundle, while `format: 'ics'` requires FullCalendar's iCalendar connector plus `ical.js`. As a result, the advertised Outlook-synced schedule will fail to populate from the feed.

- [P2] Sync upcoming games from the live calendar feed — /Users/gio/Projects/camron-finlay-baseball/js/main.js:95-95
  When `ICS_FEED_URL` is non-empty, `events` is still loaded only from `data/schedule.json`, and `renderUpcoming(events)` uses that local fallback data rather than the Outlook feed. This means the main calendar and the “Upcoming Games” sidebar will diverge once the live calendar is enabled.
